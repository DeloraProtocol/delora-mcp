# Delora API base URL
DELORA_API_URL=https://api.delora.build

# Optional API key forwarded to Delora API as x-api-key
DELORA_API_KEY=

# MCP transport: stdio (default) or http
MCP_TRANSPORT=stdio

# For MCP_TRANSPORT=http
PORT=3000
HOST=0.0.0.0
